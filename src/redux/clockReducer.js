const initialState = {
    curTime: new Date(),
    selectedArrow: null,
};

export const clockReducer = (state = initialState, action) => {
    // action.payload - new data
    switch (action.type) {
        default:
            break;
    }
    return state;
};
